services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dentist
      POSTGRES_PASSWORD: dentist
      POSTGRES_DB: dentist_crm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
